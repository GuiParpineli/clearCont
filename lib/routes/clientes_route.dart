import 'package:flutter/material.dart';

import '../widgets/navbar.dart';

class ClientesRoute extends StatelessWidget {
  const ClientesRoute({super.key});

  @override
  Widget build(BuildContext context) {
    return NavBar(
      page: Container(),
    );
  }
}
